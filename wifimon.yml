# perfsonar.yml main perfSONAR config

- name: wifimon
  become: yes
  become_user: root
  become_method: sudo
  tags: 
    - template
  hosts:
    - wifimon
  roles:
    - ansible-role-wifimon
    - ansible-role-kibana
